<?xml version="1.0" encoding="UTF-8"?>
<beans:beans xmlns="http://www.springframework.org/schema/security"
             xmlns:beans="http://www.springframework.org/schema/beans"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/security
            http://www.springframework.org/schema/security/spring-security-3.2.xsd">
    <http pattern="/" security="none"/>
    <http pattern="/security/**" security="none"/>
    <http pattern="/error/**" security="none"/>
    <http pattern="/assets/**" security="none"/>
    <http pattern="/favicon.ico" security="none"/>
    <http auto-config="true" use-expressions="true" access-denied-page="${security.denied}">
        <!--加载功能相关 -->
        <custom-filter ref="filterSecurityInterceptor" before="FILTER_SECURITY_INTERCEPTOR"/>

        <form-login
                login-page="${security.login}"
                authentication-failure-handler-ref="osAuthenticationFailureHandler"
                authentication-success-handler-ref="osAuthenticationSuccessHandler"/>

        <logout
                invalidate-session="true"
                logout-success-url="${security.login}"
                logout-url="${security.logout}"
                success-handler-ref=""/>

        <remember-me key="osAppKey"/>

        <intercept-url pattern="/**" access="isAuthenticated()"/>

    </http>

    <authentication-manager alias="authenticationManager">
        <authentication-provider ref="authenticationProvider">
        </authentication-provider>
    </authentication-manager>

    <beans:bean id="authenticationProvider"
                class="org.springframework.security.authentication.dao.DaoAuthenticationProvider">
        <beans:property name="userDetailsService" ref="orangeSideUserService"/>
        <beans:property name="hideUserNotFoundExceptions" value="false"/>
    </beans:bean>

    <!-- 加载用户相关 -->
    <beans:bean id="orangeSideUserService"
                class="com.orangeside.authorization.security.OrangeSideUserServiceImpl"/>

    <!-- 资源国际化 -->
    <beans:bean id="messageSource"
                class="org.springframework.context.support.ReloadableResourceBundleMessageSource">
        <beans:property name="basename"
                        value="classpath:/config/properties/messages_zh_CN"/>
    </beans:bean>

    <!-- 自定义验证成功后处理的拦截器 -->
    <beans:bean id="filterSecurityInterceptor"
                class="com.orangeside.authorization.security.OrangeSideSecurityInterceptor">
        <beans:property name="securityMetadataSource" ref="orangeSideSecurityMetadataSource"/>
        <beans:property name="accessDecisionManager"
                        ref="orangeSideAccessDecisionManager"/>
        <beans:property name="authenticationManager" ref="authenticationManager"/>
    </beans:bean>

    <beans:bean id="orangeSideAccessDecisionManager"
                class="com.orangeside.authorization.security.OrangeSideAccessDecisionManager"/>

    <beans:bean id="orangeSideSecurityMetadataSource"
                class="com.orangeside.authorization.security.OrangeSideSecurityMetadataSourceImpl">
        <beans:constructor-arg ref="securityService"/>
    </beans:bean>

    <!-- 自定义登陆成功handler -->
    <beans:bean id="osAuthenticationSuccessHandler"
                class="com.orangeside.authorization.security.OrangeSideSavedRequestAwareAuthenticationSuccessHandler">
        <beans:property name="securityService" ref="securityService"/>
        <beans:property name="defaultTargetUrl" value="${security.default.target}"/>
    </beans:bean>

    <beans:bean id="osAuthenticationFailureHandler"
                class="com.orangeside.authorization.security.OrangeSideSimpleUrlAuthenticationFailureHandler">
        <beans:property name="defaultFailureUrl" value="${security.login}?error=true"/>
    </beans:bean>

</beans:beans>
